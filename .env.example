# Example environment variables for IdeaForge
# Copy to .env and adjust as needed

# Enable debug logging (set to 'true' to enable verbose logs)
VITE_DEBUG=true

# API key for optional AI features
VITE_API_KEY=
